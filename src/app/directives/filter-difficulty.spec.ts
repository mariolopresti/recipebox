import { FilterDifficulty } from './filter-difficulty';

describe('FilterDifficulty', () => {
  it('should create an instance', () => {
    const directive = new FilterDifficulty();
    expect(directive).toBeTruthy();
  });
});
