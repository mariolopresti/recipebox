import { FilterByName } from './filter-by-name';

describe('FilterByName', () => {
  it('should create an instance', () => {
    const directive = new FilterByName();
    expect(directive).toBeTruthy();
  });
});
